apiVersion: v1
kind: Service
metadata:
  name: gotaskq
  labels:
    app: gotaskq
spec:
  selector:
    app: gotaskq
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
  type: ClusterIP
