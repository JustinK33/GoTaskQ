apiVersion: v1
kind: Service
metadata:
  name: conduit
  labels:
    app: conduit
spec:
  selector:
    app: conduit
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
  type: ClusterIP
